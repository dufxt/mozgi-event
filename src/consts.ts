export enum PageOverlayAnimatons {
  START = 'start',
  END = 'end',
}

export type TPageOverlayAnimation = PageOverlayAnimatons | null;

export enum MenuLinkPositions {
  LEFT = 'left',
  RIGHT = 'right',
  BOTTOM = 'bottom',
}
